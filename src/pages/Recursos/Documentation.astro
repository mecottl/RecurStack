---
import Layout from '../../layouts/Layout.astro'
import { documentationData } from '../../lib/data'
import InfoCards from '../../components/InfoCard.astro'
import '../../styles/pages.css'

---
<Layout title='Diseño'>
    <div class="cards">
        {
            documentationData.map(card => <InfoCards card={card} />)
        }
    </div>
</Layout>
