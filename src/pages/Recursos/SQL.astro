---
import Layout from '../../layouts/Layout.astro'
import {sqlData} from '../../lib/data'
import InfoCards from '../../components/InfoCard.astro'
import '../../styles/pages.css'

---
<Layout title='SQL'>
    <div class="cards">
        
        {
            sqlData.map(card => <InfoCards card = {card}/>)
        }
        </div>
</Layout> 